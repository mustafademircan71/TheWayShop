@model List<Category>
<div class="categories-shop">
    <div class="container">
        <div class="row">
            @*@for (int i = 0; i < Model.Count / 2; i++)
        {
            <div class="col-lg-4 col-md-4 col-sm-12 col-xs-12">
                @for (int j = (i * 2); j < (i * 2) + 2; j++)
                {
                    <div class="shop-cat-box">
                        <img class="img-fluid" src="images/t-shirts-img.jpg" alt="" />
                        <a class="btn hvr-hover" href="#">@Model[j].CategoryName</a>
                    </div>
                }
            </div>
        }*@


            @foreach (var category in Model)
            {
                if (category.IsActive != false)
                {
                    <div class="col-lg-4 col-md-4 col-sm-12 col-xs-12">
                        <div class="shop-cat-box">
                            <img class="img-fluid" src="@(string.IsNullOrEmpty(category.CategoryPhoto) ? "/AdminPanel/img/CategoryPhotos/no-category-image.jpg" : category.CategoryPhoto)" alt="" />
                            <a class="btn hvr-hover" href="/shop/@category.Id">@category.CategoryName</a>
                        </div>

                    </div>
                }

            }


        </div>
    </div>
</div>
