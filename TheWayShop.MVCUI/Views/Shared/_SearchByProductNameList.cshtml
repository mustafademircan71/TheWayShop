@model List<string>

@if (Model != null)
{
    <div id="divSearchByProductName" class="filter-sidebar-left" style="display:@(Model.Count == 0 ? "none":"block");">
        @for (int i = 0; i < Model.Count; i++)
        {
            <span>@Model[i]</span> <br />
        }
    </div>
}
